---
interface Props {
	title: string;
	tagline?: string;
	align?: "start" | "center";
}

const { align = "center", tagline, title } = Astro.props;
---

<div class="hero min-h-screen bg-base-200">
	<div class="hero-content flex-col lg:flex-row-reverse">
		<div class="avatar">
			<div
				class="w-96 rounded-full ring ring-primary ring-offset-base-100 ring-offset-2"
			>
				<img
					src={`${import.meta.env.BASE_URL}/assets/avatar.jpeg`}
					alt="Jeanine White asdf "
				/>
			</div>
		</div>
		<div>
			<p class="text-2xl font-bold py-6">Hi there, I'm</p>
			<h1 class="text-5xl font-bold">Luis!</h1>
			<p class="py-6">
				<article class="prose prose-lg max-w-[750px] prose-img:mx-auto">

			<slot />
				</article>
			</p>
		</div>
	</div>
</div>
